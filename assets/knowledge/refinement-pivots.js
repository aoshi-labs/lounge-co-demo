/**
 * Refinement adjacency — catalog-driven ladders replace hero-brand pivot tables.
 * Lighter/bolder steps use RecommendationSelectors.buildCatalogIntensityLadders.
 */
(function (global) {
  'use strict';

  global.PilotRefinementPivots = {
    refinementAdjacentPilot: {}
  };
})(typeof window !== 'undefined' ? window : global);
